# Superseded documentation

Everything in this directory predates the Phase 0 cleanup and is kept for history only.

It contains claims that were never true of the code, including:

- a `_generate_general_answer()` function and a "language model fallback" that never existed
- "Model Context Protocol powered" — no MCP protocol code has ever been in this repo
- "DuckDuckGo API integration" — replaced by Brave/SerpAPI/Exa
- "Production Ready"

Use the root [README](../README.md) instead.
